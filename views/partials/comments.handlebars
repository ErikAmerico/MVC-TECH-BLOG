
{{#each comments}}
  <section class="comment">
    <div class="comment-user">
      {{this.username}} on {{this.createdAt}}
    </div>
    <div class="text">
      {{this.content}}
    </div>
  </section>
{{/each}}
